/**
 * This contains all implementations of the {@link DataProvider} class.
 * Take into account that importing any of these classes will require you to have
 * a certain package installed in your project.
 * @packageDocumentation
 * @module DataProviders
 */

import LevelDataProvider from './LevelDataProvider';
import RedisDataProvider from './RedisDataProvider';

export {
  LevelDataProvider,
  RedisDataProvider
};
